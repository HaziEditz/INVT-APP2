<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khybertech.taxi360driver">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.location.gps" />
    <uses-feature android:name="android.hardware.location.network" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <service -->
        <!-- android:name="com.khybertech.360taxidriver.MainActivity.NotificationExtenderBareBonesExample" -->
        <!-- android:exported="false"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.onesignal.NotificationExtender" /> -->
        <!-- </intent-filter> -->
        <!-- </service> -->

        <activity
            android:name=".MainActivity.Whatsup"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen" />
        <activity
            android:name=".SplashSignIn"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignIn.SignIn"
             />
        <activity
            android:name=".MainActivity.MainActivity"
            android:launchMode="singleTask"

            android:singleUser="true" />
        <activity
            android:name=".JobView.JobView"
            android:launchMode="singleTask"

            android:showOnLockScreen="true"
            android:theme="@style/AppBaseTheme" />
        <activity
            android:name=".JobView.CurrentJobDetail"
         />
        <activity
            android:name=".JobView.QueueJobDetail"

          />
        <activity
            android:name=".JobView.OfferedJobDetail"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:singleUser="true" />
        <activity
            android:name=".SignIn.EmailPasswordActivity"
            android:label="@string/label_emailpassword"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps.MapsActivityJobLocation"
            android:excludeFromRecents="true"
            android:label="@string/title_activity_maps_job_location"
            android:launchMode="singleInstance"
             />
        <activity
            android:name=".Settings.Settings"
     />
        <activity
            android:name=".JobView.CompletedJobDetail"

            android:singleUser="true" />
        <activity
            android:name=".StartShift.StartShift"
            />
        <activity
            android:name=".ShiftHistory.ShiftHistory"
             />
        <activity
            android:name=".JobView.AddJob.AddJob"
             />
        <activity
            android:name=".Maps.PickAddressMap"
            android:label="@string/title_activity_pick_address_map"

            android:theme="@style/AppBaseTheme" />
        <activity
            android:name=".Maps.Pickupactivity"
            android:label="@string/title_activity_pick_address_map"

            android:theme="@style/AppBaseTheme" />
        <activity
            android:name=".Maps.DropAddressMap"
            android:label="@string/title_activity_drop_address_map"
         />
        <activity
            android:name=".Maps.DestinationActivity"
            android:label="@string/title_activity_drop_address_map"
        />
        <activity
            android:name=".MyAccountActivity"
            android:label="My Account Activity"
           />
        <activity
            android:name=".UpdateProfileActivity"
            android:label="Update Profile"
         />
        <activity
            android:name=".Chat.ChatActivity"
            android:launchMode="singleTask"
        />
        <activity
            android:name=".JobView.UpdateJob.UpdateJob"
         />
        <activity
            android:name=".Maps.TestingMap"
            android:label="@string/title_activity_testing_map"
          />

        <service
            android:name=".MainActivity.Taximetterservice"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".ShiftHistory.ShiftsHistory"
            android:label="@string/title_activity_shifts_history"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".add_extra_money"
            android:label="@string/title_activity_add_extra_money"
            android:theme="@style/Theme.AppCompat.Light.Dialog.MinWidth" />
        <activity
            android:name=".MainActivity.TaximetterActivity"
            android:label="@string/title_activity_taximetter"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity.ZonesDetails"
            android:label="All Zones"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity.FixedPrice"
            android:label="@string/title_activity_fixed_price"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".MainActivity.RideCancel"
            android:theme="@style/Theme.AppCompat.Light.Dialog.MinWidth" />
    </application>

</manifest>